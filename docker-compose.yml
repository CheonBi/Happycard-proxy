version: "3.8"

services:
  nginx:
    image: nginx:latest
    container_name: happycard_proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    restart: always
